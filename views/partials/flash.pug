if messages.errors
  .alert.alert-danger.fade.show.white-text
    button.close(type='button', data-dismiss='alert')
      i.far.fa-times-circle
    for error in messages.errors
      div= error.msg
if messages.info
  .alert.alert-info.fade.show.white-text
    button.close(type='button', data-dismiss='alert')
      i.far.fa-times-circle
    for info in messages.info
      div= info.msg
if messages.success
  .alert.alert-success.fade.show.white-text
    button.close(type='button', data-dismiss='alert')
      i.far.fa-times-circle
    for success in messages.success
      div= success.msg
